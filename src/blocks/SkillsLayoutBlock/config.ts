import { Block } from "payload";

export const SkillsLayoutBlock: Block = {
    slug:'skillsLayoutBlock',
    interfaceName:'SkillsLayoutBlock',
    fields:[
       {
            name:'customClassNames',
            type:'text'
        }
    ]
}